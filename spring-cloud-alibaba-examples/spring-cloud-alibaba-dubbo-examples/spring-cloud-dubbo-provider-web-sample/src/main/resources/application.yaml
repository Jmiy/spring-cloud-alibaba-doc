dubbo:
  scan:
    base-packages: com.alibaba.cloud.dubbo.service
  protocols:
    dubbo:
      port: -1

feign:
  hystrix:
    enabled: true

server:
  port: 38080

management:
  endpoints:
    web:
      exposure:
        include: dubborestmetadata